---
- hosts: "{{ target_group }}"  # Группа из переменной
  become: yes
  become_method: sudo
  tasks:

    - name: update
      apt: update_cache=yes

    - name: Install mc
      apt: name=mc state=latest
